import React from "react";
import { TopNav, SideNav, ProfileForm } from "./components";

export const Profile = () => {
  return (
    <div>
      <TopNav />
      <SideNav />
      <ProfileForm />
    </div>
  );
};
